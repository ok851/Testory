# 🚀 步骤录制功能快速启动指南

## 前置条件检查

### 1. 确认 Playwright 已安装
```bash
pip show playwright
```

如果未安装，执行：
```bash
pip install playwright
playwright install chromium
```

### 2. 确认平台依赖完整
```bash
pip install -r requirements.txt
```

## 启动平台

### Windows (PowerShell)
```powershell
python app.py
```

平台将在 `http://localhost:5000` 启动

## 使用步骤录制功能

### 第一步：登录平台
1. 打开浏览器访问 `http://localhost:5000`
2. 使用管理员账号登录

### 第二步：创建测试用例
1. 进入项目
2. 点击"创建用例"
3. 填写用例基本信息（如：登录功能测试）
4. 保存用例

### 第三步：开始录制步骤
1. 在用例详情页，点击 **🎬 开始录制** 按钮
2. 在弹出窗口中输入目标 URL
   ```
   例如：https://www.baidu.com
   ```
3. 点击 **▶ 开始录制**

### 第四步：执行操作
系统会自动打开浏览器，此时：
1. 在打开的浏览器中进行正常操作
2. 系统会实时捕获您的操作并显示在预览区

**示例操作：**
```
1. 点击搜索框 (#kw)
2. 输入 "UI 自动化测试"
3. 点击搜索按钮 (.s_btn)
4. 查看搜索结果
```

### 第五步：查看实时预览
录制过程中，面板会实时显示：
```
┌──────────────────────────────────────┐
│ 📋 实时步骤预览                       │
├──────────────────────────────────────┤
│ 👆 步骤 1: 点击 #kw                   │
│ ⌨️ 步骤 2: 在 #kw 中输入：UI 自动化测试  │
│ 👆 步骤 3: 点击 .s_btn                │
└──────────────────────────────────────┘
```

### 第六步：结束录制
有两种方式：

**方式 A：直接保存**
- 点击 **💾 保存步骤到用例**
- 步骤自动保存到当前用例

**方式 B：先停止再保存**
1. 点击 **⏹ 停止录制**
2. 预览步骤无误后点击 **💾 保存步骤到用例**

### 第七步：验证和执行
1. 返回用例步骤列表
2. 查看自动生成的步骤
3. 可以编辑、删除或调整顺序
4. 点击 **▶ 运行用例** 执行测试

## 完整示例：录制百度登录流程

### 场景描述
录制用户登录百度搜索平台的完整流程

### 操作步骤

#### 1. 开始录制
- URL: `https://www.baidu.com`
- 点击"开始录制"

#### 2. 执行操作序列
```
1. 点击右上角"登录"链接
2. 在用户名输入框输入：testuser
3. 在密码输入框输入：password123
4. 点击"登录"按钮
5. 等待登录成功提示
```

#### 3. 生成的步骤
系统自动生成以下测试步骤：
```sql
步骤 1: 点击元素 - 登录链接
  - 定位器：#s-top-loginbtn
  - 类型：click

步骤 2: 输入用户名
  - 定位器：#TANGRAM__PSP_11__userName
  - 值：testuser
  - 类型：fill

步骤 3: 输入密码
  - 定位器：#TANGRAM__PSP_11__password
  - 值：password123
  - 类型：fill

步骤 4: 点击登录按钮
  - 定位器：#TANGRAM__PSP_11__submit
  - 类型：click
```

#### 4. 保存并执行
- 点击"保存步骤到用例"
- 步骤自动添加到用例
- 点击"运行用例"验证

## 故障排查

### 问题 1：点击录制按钮无反应
**可能原因：**
- 浏览器未正确安装
- Playwright 驱动缺失

**解决方案：**
```bash
playwright install chromium
playwright install-deps chromium
```

### 问题 2：录制时浏览器闪退
**可能原因：**
- 系统内存不足
- 浏览器版本不兼容

**解决方案：**
- 关闭其他占用内存的程序
- 重新安装 Playwright：
  ```bash
  pip uninstall playwright
  pip install playwright
  playwright install chromium
  ```

### 问题 3：录制的步骤无法执行
**可能原因：**
- 元素定位器变化
- 页面加载时间不足

**解决方案：**
1. 手动编辑步骤，调整定位器
2. 在步骤前添加等待操作
3. 使用更稳定的元素定位方式

### 问题 4：无法捕获某些操作
**限制说明：**
- 文件选择对话框操作暂不支持
- 跨域弹窗可能需要特殊处理
- 某些 Canvas 操作可能无法捕获

**替代方案：**
- 手动添加相关步骤
- 使用平台的其他步骤创建方式

## 性能优化建议

### 1. 分段录制
对于复杂流程（超过 20 步），建议：
- 分成多个小段录制
- 每段录制完成后保存
- 最后组合所有步骤

### 2. 定期清理
- 定期删除无效录制会话
- 清理浏览器缓存
- 重启平台服务释放资源

### 3. 网络优化
- 确保网络连接稳定
- 避免在网络高峰期录制
- 使用本地测试环境

## 高级配置

### 修改浏览器配置
如需自定义浏览器启动参数，编辑 `step_recorder.py`:

```python
self.browser = await self.playwright.chromium.launch(
    headless=False,  # 改为 True 可隐藏浏览器
    args=[
        '--window-size=1920,1080',  # 自定义窗口大小
        '--start-maximized',  # 最大化窗口
    ]
)
```

### 调整轮询间隔
修改前端 JavaScript 中的轮询频率（默认 2 秒）:

```javascript
recordingInterval = setInterval(pollRecordingSteps, 2000); // 单位：毫秒
```

### 自定义步骤生成逻辑
编辑 `step_recorder.py` 中的 `_generate_step` 方法，可以：
- 添加新的操作类型支持
- 优化定位器生成策略
- 自定义步骤描述格式

## 安全注意事项

### 1. 敏感信息保护
- ❌ 不要录制密码等敏感信息
- ✅ 使用数据驱动方式填充敏感数据
- ✅ 录制后手动添加敏感数据步骤

### 2. 环境隔离
- 建议在测试环境录制
- 避免在生产环境进行录制操作
- 录制前备份重要数据

### 3. 权限管理
- 仅授权用户可使用录制功能
- 定期审查录制历史
- 控制录制功能的访问权限

## 下一步

掌握基础用法后，可以尝试：
1. 📖 阅读完整文档 `STEP_RECORDER_GUIDE.md`
2. 🎯 学习高级录制技巧
3. 🔧 自定义录制配置
4. 🤖 结合定时任务自动执行

---

**祝您使用愉快！**

如有问题，请查看完整文档或联系技术支持。
