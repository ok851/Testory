python-dotenv==1.0.1
Flask==2.3.3
fastapi==0.110.0
uvicorn[standard]==0.27.1
Flask-CORS==4.0.0
Flask-Login==0.6.3
Werkzeug==3.0.1
# playwright 1.40 pins greenlet==3.0.1; on cp313 use newer playwright with prebuilt wheels.
playwright>=1.48.0,<2; python_version >= "3.13"
playwright==1.40.0; python_version < "3.13"
# numpy 1.26.x has no cp313 wheels on Windows; pip may build from sdist (needs MSVC/GCC).
# Python 3.13+: use numpy 2.x / newer opencv with official wheels.
numpy>=2.1.0,<3; python_version >= "3.13"
numpy==1.26.4; python_version < "3.13"
opencv-python>=4.12.0.88,<5; python_version >= "3.13"
opencv-python==4.10.0.84; python_version < "3.13"
APScheduler==3.10.4
openpyxl==3.1.2
reportlab==4.4.10
# Postman-style pre/post scripts (Duktape JS)
dukpy>=0.4.0
# Requirement doc extraction (PDF / Word)
pypdf>=4.0.0
python-docx>=1.1.0
# Windows desktop automation (see requirements-windows.txt)
pywin32>=306; sys_platform == "win32"
pywinauto>=0.6.8; sys_platform == "win32"
pyperclip>=1.8.2
pyautogui>=0.9.54; sys_platform == "win32"
mss>=9.0.0; sys_platform == "win32"
psutil>=5.9.0
# HTTP client (app.py and many modules; must be in offline bundle)
requests>=2.31.0
# Optional captcha — NOT in main/offline installer (see requirements-captcha-optional.txt):
#   pip install -r requirements-captcha-optional.txt
#   or POST /api/captcha/optional-deps/install  {"component":"ddddocr"}
#   then CAPTCHA_DDDDOCR_ENABLE=1 in .env and restart
# VLM fallback: LOCAL_VISION_ENABLE=1 + ollama pull llava (CAPTCHA_VISION_FALLBACK=1, CAPTCHA_VISION_TIMEOUT=25)
urllib3>=2.0.0,<3
certifi>=2024.2.2
charset-normalizer>=3.3.0
idna>=3.6
# Web capture extension WebSocket bridge
websockets>=12.0
