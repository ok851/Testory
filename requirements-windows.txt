# Windows desktop dependencies (install with requirements.txt)
-r requirements.txt
pywin32>=306
pywinauto>=0.6.8
pyperclip>=1.8.2
pyautogui>=0.9.54
mss>=9.0.0
psutil>=5.9.0
# Desktop shell window (packaging/uat_desktop.py)
pywebview>=5.0
# 可选：内嵌 Hermes Agent（Testory AI；离线安装包默认由 Flask bootstrap 拉起）
# pip install hermes-agent
hermes-agent>=0.16.0,<0.17
