# 2026-07-17 工作日志

## AI自主测试重构计划评估

用户提交了一份《AI自主测试页面重构计划》，要求评估完成度与缺陷。

### 完成审计
- 使用 Explore agent 对 9 个核心 AI 模块 + 70+ API 端点做了全量代码审计
- 关键发现：计划声称"新增"的 10 个 API 中有 7 个已存在，大量能力被重复设计

### 核心结论
- 作为通用设计文档：完成度 ~75%
- 作为针对现有代码库的重构计划：完成度 ~25%
- 根本问题：计划脱离现有代码库，像给空白项目写设计而非给成熟系统写重构

### 已存在的关键能力（计划忽略或重复）
- `ai_chat_tool_loop.py`（787行）：完整多轮 tool calling，含 hermes_execute + refine_test_plan + SSE 流式
- `ai_step_normalization.py`（837行）：三平台 action 全映射 + 智能修复 + Playwright 转换
- `ai_page_probe.py`（2000行）：50+ UI框架 DOM 探测 + iframe/Shadow DOM + probe 注册表
- `ai_selector_recovery.py`：4层选择器降级（缓存→VLM→LLM→Hermes）
- `ai_vision_local.py` + `ai_vision_grounding.py` + `ai_vision_insight.py`：完整视觉（OCR+VLM+云端回退+熔断器）
- `ai_memory_store.py`：向量记忆（embedding + cosine Top-K）
- `ai_test.html`：已有三列布局 + 玻璃态 + 深色模式 + screen-share toggle
- 已存在 API：/api/ai/task/execute, /api/ai/actions/to-case, /api/ai/vision/capture, /api/ai/screen-share/toggle 等

### 最危险漏洞
1. 移除画布 → CDP 依赖断裂 → tool loop 的 system prompt 依赖 CDP attach 状态
2. 同步视觉流水线 → 每个动作截图+OCR+VLM → 执行速度降 5-10 倍
3. 共享屏幕 → 隐私/安全/合规完全未讨论
4. 前端深色渐变 → 与浅色桌面壳（frameless + CSS !important）冲突

### 建议
需要将计划从"新建清单"改为"整合清单"，先解决 CDP 依赖迁移方案再动画布。

## 重写 AI 自主测试整合方案

用户要求重写一份贴合现有代码的整合方案，认真思考整个 agent 工作链流程。

### 深入代码审计
精读了以下关键文件以理解工作链：
- `ai_chat_tool_loop.py` 全文（787行）：多轮 tool calling 循环，`hermes_execute_allowed()` 已有"无 CDP 也能执行"路径
- `hermes_config.py` 全文（275行）：`sync_hermes_cdp_endpoint()` 可同步任意 CDP URL 到 Hermes（热更新 API `POST /v1/config/cdp`）
- `app.py:5465` `/api/ai/agent/gateway-stream`：已有"主 Playwright 回退"模式
- `app.py:5936` `/api/ai/task/execute`：完整 SSE 流式，调用 `run_ai_chat_with_tools_stream`
- `app.py:6320` `/api/ai/actions/to-case`：naive 映射，完全没用 `ai_step_normalization.py`
- `app.py:6392-6412` 视觉/截屏 API：全部是 stub（"功能开发中"）
- `templates/ai_test.html`：已有三列布局+玻璃态+深色模式+screen-share toggle+渐变背景

### 关键发现
1. `hermes_execute_allowed()` 第 84-91 行：Hermes Gateway 健康 → 允许执行（无需 CDP）
2. `sync_hermes_cdp_endpoint(cdp_ws, restart_gateway=False)` → `_try_hot_update_cdp()` → `POST /v1/config/cdp` 可热更新
3. 现有前端已有 screen-share toggle，但后端是 stub
4. `/api/ai/actions/to-case` 只做了 `action_type→action, target→target` naive 映射

### 方案产出
写入 `AI自主测试整合方案.md`：
- 核心原则：先复用后新增；移除画布=隐藏UI保留引擎；视觉按需触发；动作转用例走现有管线
- 完整 agent 工作链 as-is vs to-be 对比
- 模块整合清单：复用 18 个模块（6000+行）、修改 6 个路由+1个模板、新增 2 个模块（~450行）
- 新增模块设计：`ai_external_browser_bridge.py`（有头Playwright+CDP）、`ai_action_recorder.py`（Hermes文本解析+按需视觉）
- 5 个 stub/naive 路由的修改详细方案（含代码）
- 前端策略：保留三列骨架，只替换中栏 iframe 为对话面板
- 6 个实施阶段
- 与原计划的关键差异对比表
