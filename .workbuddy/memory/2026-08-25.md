# 2026-08-25 工作日志

## Task #5 完成：Browser 工具重复调用优化（.venv 补丁）
- 新建 `patch_venv_hermes_tools.py`：对 .venv 依赖包做幂等补丁（带 `[Testory-patch]` 标记，可重复执行）
- 补丁内容：
  1. `tools/browser_tool.py`：只读工具短 TTL 缓存（snapshot 3s / console 5s，命中返回 `"cached": true`，缓存上限 96）；`browser_navigate` 自动快照写缓存；`_run_browser_command` 页面变更命令自动失效该任务缓存；`browser_console` 空结果加引导文案（提示带 expression）；`_browser_eval` 失效缓存
  2. `agent/tool_guardrails.py`：`hard_stop_enabled` 默认 True
- 修复两个脚本缺陷：① 缓存 TTL 按各自类型存储判断（`Dict[str, Tuple[ts, ttl, payload]]`）；② 清理逻辑 `>=` + 超上限按最旧淘汰；③ skip 语义返回 True（幂等重跑 exit 0）
- 验证：8/8 功能断言通过（put/get/失效/TTL/上限/guardrails/snapshot 分支），幂等重跑全 skip exit 0
- 注意：`time.time()` 在 Windows 上连续调用可能同值（diff=0），ttl 判断需注意边界

## Task #7 完成：同屏双端观察（WebCodecs 播放器 + cross_end 分屏）
- 从 git `aee8ecc` 恢复 `static/js/mobile_scrcpy_mirror.js`（827 行，ScrcpyMirrorPlayer：WebCodecs 解码、Annex-B→AVCC、`[meta:u8][len:u32][payload]` 帧解析、WS/HTTP 双通道、内置重连、sendTap/sendSwipe 反控）
- 新建 `templates/mobile_h264_player.html`：手机 H.264 实时观察台（/mobile-h264-player?serial=xxx）
  - 设备下拉（/api/mobile/devices）、桥接探测（ws://host:8767/scrcpy）、WebCodecs 检测徽章、FPS 显示
  - canvas 点击 = 手机 tap 反控（按 CSS 比例换算坐标）
  - iframe 内嵌时自动隐藏导航栏全高适配
- `app.py` 新增 `/mobile-h264-player` 路由（注入 bridge_port）
- `templates/cross_end.html` 加「双端观察」按钮 + 全屏 overlay：左 PC 端画面（/api/screenshot 3s 轮询）+ 右手机 H.264 流（iframe 内嵌播放器），含 PC 手动刷新、手机全屏新窗口
- 验证：app.py py_compile OK、两模板 Jinja 渲染 OK、播放器/内联 JS node --check OK
- 关键协议：scrcpy bridge 帧 `[meta:u8][len:u32 大端][payload]`，meta 0=P/1=config(SPS/PPS)/2=keyframe；上行控制 `{"type":"tap"|"swipe","x","y","screen_width","screen_height"}`
- bridge WS 无鉴权（ws://host:8767/scrcpy?serial=xxx），页面路由走 Flask session

## Task #6 完成：设备级互斥锁 + 跨端并行编排
- 新建 `mobile_device_lock.py`：设备级互斥锁（按 serial hash 分文件 `data/.uat_mobile_dev_<sha256[:16]>.lock`）
  - 语义对齐 execution_lock：同线程可重入（深度计数）、跨线程/跨进程互斥、stale 检测（1h）、`mobile_device_guard` 上下文管理器（required=True 失败抛 MobileDeviceLockError）
  - `_DeviceLockRegistry` 按 serial 管理实例；`mobile_locks_status()` 巡检
- `mobile_cross_end_tools.py`：`_mobile_execute_action` 各动作分支（tap/swipe/input/back/home）接入设备锁（函数内 import，锁 timeout 150-180s 覆盖 APK job 等待）；APK job 是用户级队列（`enqueue_mobile_job` 强制 device_id 置空），`mobile_run_steps` 不加 serial 锁（PC 侧无法预知领取设备），真实互斥靠整机锁+会话纪律
- `multi_device_scheduler.py` 新增跨端并行编排：
  - `is_cross_end_parallel_stage(stage)`：识别 `cross_end_parallel:true` / `parallel:{pc,mobile}` 简写 / `branches` 列表
  - `_execute_pc_branch`：check_desktop_preflight + run_desktop_step_with_optional_heal 逐步骤（复用桌面自愈执行器）
  - `_execute_mobile_branch`：指定 device_id 或自动发现，走 `_execute_steps_on_device`（APK job 优先）
  - `execute_cross_end_parallel_stage`：ThreadPoolExecutor 并行双分支，allow_partial 支持，变量抽取带分支前缀
- `orchestrator.py`：顶部导入 `is_cross_end_parallel_stage`；stage 分发最前插入跨端并行分支（`cross_end_parallel_summary` 摘要）
- 验证：设备锁 16/16（重入/互斥/guard/多设备隔离/错误路径）、跨端并行 15/15（判断/解析/错误路径/orchestrator 集成）、并行性 7/7（monkeypatch 双分支 sleep 1s 总耗时 1.02s < 串行 2s）

## 备注
- 任务列表：Task #6（P2 设备级互斥锁 + 跨端并行编排）仍 pending，下次会话推进
- ✅ 全部 7 个任务完成（P0 死工具修复 → P1 移动端手/眼/同屏观察/Browser 优化 → P2 设备锁+跨端并行）
