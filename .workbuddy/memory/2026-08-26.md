# 2026-08-26 工作日志

## 仓库整理 Phase 1（删除一次性脚本）已执行
- 删除根目录 93 个一次性 .py 脚本：`_test_`(41) / `_fix`(28) / `_temp_`(9) / `_impl`(5) / `_verify`(3) / `_add_`(1) / `_find_`(2) / `_probe`(1) / `_ime_`(3)
- 删除调试产物：`_desktop_err.txt` / `_desktop_out.txt` / `_diag_out.txt` / `app_test_output.log` / `final_*.png`(3) / `test_index.png`
- 散落说明稿迁入 `docs/`：`模拟器ADB配置说明.md`、`模拟器连接功能说明.md`、`AI自主测试整合方案.md`
- 清空 `__pycache__`（203 个陈旧 .pyc）
- 根目录 .py 数：284 → 191
- 验证：无残留一次性脚本；生产模块（app.py/database.py/ai_chat_tool_loop.py/mobile_routes.py 等）完整；grep 确认无生产代码 import 这些脚本
- 删除方式：`git rm`（_test_/_fix，69 个，已 staged）+ `rm`（其余 24 脚本+调试产物，unstaged）。git 跟踪可回滚
- 注意：安全 hook `rm` 每轮限 50 文件（scope:turn），`git rm` 不触发该 hook

## Phase 2（模块迁入 platform/）暂未执行——风险决策中
- 计划要迁入 ~120 个生产模块 + 保留同名 shim；但根目录仍会留 ~120 个 shim，瘦身收益有限
- 风险点：`import *` shim 丢失 `_` 私有名；ai/desktop/mobile/hermes 间循环 import；PyInstaller glob `*.py` 对 platform/ 子包追踪未验证；pytest 本环境不可用无法跑回归
- "测试数据 json" 实际不存在：tests/ 0 json；根目录 4 json 是配置；data/ 6 json 是运行时配置（instance_id/machine_id/ai_model_registry 等）

## Phase 2（模块重组，commit 5757dd3）已执行 — 用户拍板放弃 shim 方案，改为直接迁移 + import 重写
- 用户指示：目标就是按模块分类整理，不一定要 platform/。选定方案：新建 `modules/` 顶级包（9 域子包），173 个根模块 `git mv` 迁入，**全库 1741 条 import（308 文件）重写为 `modules.*` 绝对导入，零 shim**
- 放弃原计划 `platform/` 包名的关键原因：`platform` 是 Python 标准库模块名，本地包会遮蔽 stdlib，.venv 里 adodbapi 等大量三方库 `import platform` 会崩（原计划的致命缺陷）
- 根目录最终 12 个 .py：app.py、database.py + 10 个 CLI 工具脚本（按路径调用，不迁）
- 摸排结论：全库 import 全静态（唯一动态点 app.py:20301 上方有静态 import）；~30 个模块共 41 处 `__file__` 根锚定需改 `parents[2]`
- 特殊修补：app.py 3 处 `__import__('...')` 字符串模块名；`_spec_common.py` 硬编码 hiddenimports 改点路径 + collect_submodules 加 "modules"；`launch_checks.py` components_manager 路径检查兼容双布局；`desktop_picker.py` 子进程注入 PYTHONPATH
- 踩坑记录：① str.replace 规则链中 `with_name` 规则的输出 `parents[2]` 会被后续 `resolve().parent` 规则再次匹配 → 产生 `parents[2]s[2]` 语法错误（1 处，已修）② BOM 文件在锚定+import 两阶段各前置一次 \ufeff → 三重 BOM（2 处，已归一化）
- 验证体系（无 pytest 环境的替代方案）：AST 全库解析（594 文件 0 语法错误/0 本地 import 缺失）+ .venv 真实解释器导入冒烟（13 模块 OK）+ install_root/resource_root/license.key 路径断言
- 死代码清理追加：6 个零引用根模块（desktop_capture_click_guard/platform_pay_token/screen_observer_v2/timeline_routes/unified_element_locator/mobile_logcat）+ 3 个孤儿测试（test_mobile_image_engine/test_mobile_playground/test_mobile_replay_vision，其被测模块早在 ad118bb 提交移除）
- 两个提交边界：d3b8754（Phase 1 清理）/ 5757dd3（Phase 2 重组），均可独立 revert
- 遗留建议：下次打包前跑一次 PyInstaller 构建实测（collect_submodules("modules") 是否全部入包）；pytest 环境配好后跑核心回归子集

## 桌面计划「修复 AI 多端联动」核对结论 — 计划已过时，三大根因均已修复
- 用户桌面计划文件《修复 AI 多端联动：实时用例 DOM 慢 跨端与共享屏幕.md》描述的根因与当前代码不符，修复已落在重组前提交（114e8c6 / f04dc47 / 37a1bd9 / 3c2fe06 / 98a8cf5）
- 逐项核实（全过）：①录制器已有 `_OBSERVATION_TOOL_NAMES` 黑名单 + `lift_console_expression` 提升；②`get_rich_page_context` 已单次 wait + `get_dom_context_pack(skip_wait=True)` + VLM 条件化；③`ensure_bridge_started` 生产调用点 5 处（app.py:2680 h264 路由 / mobile_routes.py:307 `_mirror_payload` / :576 status / app.py:9927 screen-share 后台 warm / `_warm_scrcpy_once`）；`_mirror_payload` 返回 mirror_ws_url+bridge_ok+warm 状态；`snapshot_connected_hands` phone=sync 配对 OR ADB serial；MOBILE_TOOL_NAMES 13 工具齐
- 运行时冒烟（实测）：ensure_bridge_started→True 监听 0.0.0.0:8767；OCR tesseract 可用；websockets 16.0；scrcpy 插件已装（AppData\Local\Testory\extensions\android\scrcpy）；static/vendor/scrcpy-server 在；12 个关键模块导入链 OK；端口 5000/8767 空闲
- 前端链路核对：openObserver→iframe `/mobile-h264-player?serial=`→Flask 路由 ensure+warm→前端 POST `/api/mobile/mirror/ensure-bridge`（mobile_routes.py:516 存在）→WS `ws://host:8767/scrcpy?serial=`
- **发现真实残留问题：tests/ 里 274 处旧路径 `mock.patch("old_module.xxx")` 字符串目标全部失效（重组后遗症，~30 个测试文件），pytest 回归安全网失效；生产代码唯一旧引用在 src-tauri/target/debug/_up_/（构建产物非活代码）**
- 结论：若用户仍见失败，多半是①当时跑的旧代码/服务未重启 ②设备侧条件（APK 无障碍 PcRunJobPoller 心跳过期→MOBILE_POLLER_STALE、中文输入必走 APK）③tests 断网无法回归
- `.env` 里 `SCRCPY_PATH=C:\Tools\scrcpy\scrcpy.exe` 指向不存在的路径（实际走插件市场安装路径，无害但易误导）
- 补充核对（前端 overlay 链）：`CE.openObserver` 主路径是 overlay 内 `<iframe src="/mobile-h264-player?serial=">`（cross_end.html:2140），`window.open` 只是可选大窗；手机 serial 取 `connection-status.udid`（`get_connected_udid()`，仅 connect/sync 后 set_connected_udid 赋值）+ `devices` 列表兜底（`mobile_routes.py:719` 的 `real_devices=list_devices_for_ui('real')` 基于 ADB，纯 ADB 也能取到）→ 前后端判定不对称被兜底弱化，非确定断点。同屏不生效真实候选收敛到：①h264 页前端 JS 未触发 `POST /api/mobile/mirror/ensure-bridge` → WS 连不上黑屏 ②ADB 未授权致 devices 空 → fail 提示 ③SCRCPY_PATH 坏路径（无害）
